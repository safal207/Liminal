# AetherProof × LIMINAL v0.1 — Ready for Schema Branch

The current LIMINAL-side artifacts are aligned with the latest co-author direction:

- RFC 8785 JCS canonicalization;
- native receipt v1.2 with `signed_extensions_hash` in the signed preimage;
- extensible registry-bound `purpose` semantics;
- mandatory rejection of policy-decision replay;
- explicit separation between core cryptographic verification and context verification.

Ready to map these artifacts into the AetherProof schema branch and resolve the remaining versioning, parent reference, actor typing, and normative/informative placement questions.
