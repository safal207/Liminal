name: Live Gonka Induced Recovery Once

on:
  push:
    branches:
      - agent/recovery-routing-v0-1
    paths:
      - .github/workflows/live-gonka-induced-recovery-once.yml

permissions:
  contents: read

jobs:
  live-gonka-induced-recovery:
    runs-on: ubuntu-latest
    environment: live-provider-trace
    env:
      GONKA_BROKER_API_KEY: ${{ secrets.GONKA_BROKER_API_KEY }}
      GONKA_BROKER_URL: ${{ secrets.GONKA_BROKER_URL }}
      OPENAI_MOCK_ONLY: "false"
      OPENAI_FALLBACK_TO_LOCAL: "false"
      PYTHONPATH: ${{ github.workspace }}:${{ github.workspace }}/src

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Require Gonka broker configuration
        shell: bash
        run: |
          set -euo pipefail
          test -n "${GONKA_BROKER_API_KEY:-}" || { echo "GONKA_BROKER_API_KEY GitHub Secret is required."; exit 1; }
          test -n "${GONKA_BROKER_URL:-}" || { echo "GONKA_BROKER_URL GitHub Secret is required."; exit 1; }

      - name: Install minimal live-trace dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install "openai>=1.30,<3" "httpx>=0.24" "structlog>=23.1"

      - name: Run Gonka induced-recovery live provider trace
        run: |
          python scripts/run-live-provider-trace.py \
            --provider gonka \
            --model "MiniMaxAI/MiniMax-M2.7" \
            --context-window-tokens "180000" \
            --max-output-tokens "1024" \
            --probe-mode induced-recovery \
            --latency-budget-seconds "20" \
            --output artifacts/live-provider-induced-recovery-trace.json

      - name: Upload evidence artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: live-gonka-induced-recovery-trace-once
          path: artifacts/live-provider-induced-recovery-trace.json
          if-no-files-found: warn
          retention-days: 14
