name: Trusted Recovery Environment Policy Once

on:
  push:
    branches:
      - agent/recovery-routing-v0-1
    paths:
      - '.github/workflows/trusted-recovery-environment-policy-once.yml'

permissions:
  contents: read
  id-token: write
  attestations: write

jobs:
  call-trusted-proof:
    uses: ./.github/workflows/trusted-recovery-decision-proof.yml
    secrets: inherit
