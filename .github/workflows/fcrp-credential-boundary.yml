name: FCRP Credential Boundary

on:
  push:
    branches: [main, master, develop]
  pull_request:
    branches: [main, master, develop]

permissions:
  contents: read

jobs:
  credential-boundary:
    uses: safal207/ContractGraph-QA/.github/workflows/credential-boundary-reusable.yml@6baf9e8396ad216556a4e932356b2aab080aaffe
    permissions:
      contents: read
    with:
      scanner-ref: 6baf9e8396ad216556a4e932356b2aab080aaffe
