# Normalize text files
* text=auto

# Shell scripts must be LF for Linux/CI
*.sh text eol=lf

# PowerShell and batch stay CRLF on Windows
*.ps1 text eol=crlf
*.bat text eol=crlf

# Python sources as LF (safer cross‑platform)
*.py text eol=lf

# Markdown keep default normalization
*.md text eol=lf

# Ensure YAML is normalized
*.yml text eol=lf
*.yaml text eol=lf

# JSON snapshots should stay text for readable diffs
*.json text eol=lf

# Force adapter logs to be treated as UTF-8 text
docs/research/ADAPTER_CHECK_RESULTS_20250803.md text eol=lf
