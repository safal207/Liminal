[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "flake8_shim"
version = "0.1.0"
description = "Flake8 shim for development"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
packages = ["flake8_shim"]
